import 'package:flutter/material.dart';

class AppColors {
  static const BlakMode = Color.fromARGB(255, 0, 0, 0);

  static const LightMode = Color.fromARGB(255, 80, 17, 13);

  static const textG = Color.fromARGB(255, 134, 127, 127);

  static const primary = Color(0xff50110D);
}
